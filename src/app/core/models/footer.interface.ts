export interface ContactUs {
    icon: string;
    title: string;
    text: string;
    link: string;
}

export interface SocialNetworks {
    icon: string;
    link: string;
    target: string;
}

export interface Links {
    title: string;
    items: any[];
}